sudo: required

language: php

php:
  - "7.1"
  
env:
  - WP_VERSION=latest
  
before_script:
    - export PLUGIN_SLUG=$(basename $(pwd))
    - git clone git://develop.git.wordpress.org/ /tmp/wordpress
    - mkdir -p /tmp/wordpress/wp-content/plugins/$PLUGIN_SLUG/tests
    - cp -a src/. /tmp/wordpress/wp-content/plugins/$PLUGIN_SLUG
    - cp -a tests/. /tmp/wordpress/wp-content/plugins/$PLUGIN_SLUG/tests
    - cp -a phpunit.xml /tmp/wordpress/wp-content/plugins/$PLUGIN_SLUG
    - mysql -e "CREATE DATABASE wordpress_tests;" -uroot
    - cd /tmp/wordpress
    - cp wp-tests-config-sample.php wp-tests-config.php
    - sed -i "s/youremptytestdbnamehere/wordpress_tests/" wp-config.php
    - sed -i "s/yourusernamehere/travis/" wp-config.php
    - sed -i "s/yourpasswordhere//" wp-config.php
    - cd "/tmp/wordpress/wp-content/plugins/$PLUGIN_SLUG"
    
script: phpunit
